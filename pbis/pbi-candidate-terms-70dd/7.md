# 7 Purge Adena-derived canonical language for clean-slate validation

Back to task list: `tasks.md`

## Overview

Purge all Adena-derived canonical-language state so that Tasks 8 and 9 can exercise the new pipeline from a true zero state. Both Adena and System B will enter Task 9 from **identical** pre-state: their source vocabulary is preserved as a fact, but nothing in `semantic_modeling`, and only seed-root concepts in `core_semantics.canonical_concepts`. This is the only way the cross-source convergence story can be honestly demonstrated — every canonical concept that exists post-Task-9 must have arrived via the new machinery.

This task replaces the original "backfill 38 Adena deferrals" plan. The 38 deferrals were prior-process artifacts; backfilling them would smuggle pre-machinery judgments back into the new system. The cleaner test is: purge them, and let the new pipeline re-derive (or not re-derive) the same decisions through audited machinery.

### What if we didn't do this step?

**Immediate Impact:** Task 8 would run against a polluted state. Q8 would show prior Adena candidates as `candidate`/`deferred`/`promoted` even though those decisions never went through the new pipeline. The validation in Tasks 8 and 9 would be hollow — we'd be measuring metadata, not machinery.

**Downstream Consequences:** Cross-source convergence in Task 9 cannot be honestly demonstrated if half the convergent terms are already canonical concepts from a different process. The PBI's value proposition fails its own acceptance test.

**Technical Debt:** Two parallel histories (the prior ad-hoc process and the new machinery) coexist in `core_semantics.canonical_concepts`, indistinguishable from each other. Future audits cannot determine which concepts were earned and which were grandfathered.

**Alternative Approaches:** Backfill prior decisions as `status='promoted'` / `status='deferred'` candidate_terms with synthetic provenance. Rejected — it gives prior decisions the metadata of having gone through the machinery without actually exercising it.

**Risk Assessment:** Medium — destructive operation against `canonical_concepts` and `mapping_definitions`. Reversible only by re-running the prior ad-hoc process. Operator confirmation required before execution.

## Description

Author one operational script `peddlerr/workbench/scripts/reset_canonical_language.py` that:

1. **Snapshots current state** — counts and (optionally) JSON-dumps every row that will be deleted. Snapshot is written to a timestamped file under `peddlerr/workbench/scripts/data/` for audit.
2. **Asserts pre-conditions:**
   - Every `core_semantics.canonical_concepts` row with `parent_concept_id IS NULL` is in the known seed-root set: `location_type`, `org_type`, `person_role`, `healthcare_offering`, `entity_relationship`. If any other root exists, halt and report.
   - No `core_semantics.mapping_definitions` references vocabulary_terms from sources other than `adena-health-system`. If they do, halt and report (this PBI assumes Adena is the only ingested source).
3. **Prints the purge plan** — table of {table_name, count_to_delete} — and waits for `--confirm`. No `--confirm` → exit without writes.
4. **Executes the purge in one transaction:**
   - `DELETE FROM core_semantics.mapping_definitions` (all rows).
   - `DELETE FROM core_semantics.canonical_concepts WHERE parent_concept_id IS NOT NULL` (everything except seed roots).
   - `TRUNCATE semantic_modeling.candidate_terms RESTART IDENTITY CASCADE` (which also truncates `candidate_term_observations` via FK).
5. **Verifies post-state:**
   - `canonical_concepts` count = number of seed roots (should be 5 absent surprises).
   - `mapping_definitions` count = 0.
   - `candidate_terms` count = 0; `candidate_term_observations` count = 0.
   - `core_semantics.semantic_model_versions`, `source_vocabulary_models`, `vocabulary_surfaces`, `vocabulary_terms` all unchanged (Adena's published vocabulary is a fact, not derived state).
6. **Logs a final summary** with row deltas and the path to the snapshot file.

The script is **one-shot** — designed to be run once at the start of Task 7's execution and never again unless an operator explicitly chooses to reset. It is committed for auditability and reproducibility, not as runtime infrastructure.

## Control-Plane / Execution Context

- `pbi_id`: `pbi-candidate-terms-70dd`
- `task_id`: `7`
- `status`: `InProgress`
- `branch`: `pbi-candidate-terms-70dd/task-7-purge`
- `worktree`: `/Users/briankerschner/repos/peddlerr` (main worktree)
- `session`: `claude-opus-4-7-1m-20260426`

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2026-04-25 15:21:39 | Created | N/A | Proposed | Task created from candidate-terms PBI scope | AI_Agent |
| 2026-04-26 10:00:00 | Rewrite | Proposed | Proposed | Reframed from "backfill 38 deferrals" to "purge Adena-derived canonical language" so Tasks 8/9 can validate the new pipeline against a clean slate | User |
| 2026-04-26 15:20:00 | Status Update | Proposed | InProgress | Started Task 7 work on branch `pbi-candidate-terms-70dd/task-7-purge` after PR #9 merge; pre-state confirmed (20 canonical_concepts / 5 seed roots / 48 mapping_definitions / 73 candidate_terms / 78 vocabulary_terms preserved) | User |

## Requirements

1. Script lives at `peddlerr/workbench/scripts/reset_canonical_language.py`.
2. Pre-condition asserts: only known seed roots in `canonical_concepts`; no non-Adena `mapping_definitions`. Halts with clear error if violated.
3. Snapshot of pre-purge state written to `peddlerr/workbench/scripts/data/canonical_reset_snapshot_<timestamp>.json`.
4. `--confirm` flag required to execute purge; absence prints the plan and exits cleanly.
5. Purge runs in a single transaction.
6. Post-state verified inline: zero rows in `mapping_definitions`, `candidate_terms`, `candidate_term_observations`; only seed roots in `canonical_concepts`.
7. `core_semantics.semantic_model_versions`, `source_vocabulary_models`, `vocabulary_surfaces`, `vocabulary_terms` are untouched.
8. Re-running with `--confirm` against an already-purged DB is idempotent (zero deletions, post-state still valid).
9. Snapshot file is committed to the PR for audit (one snapshot per execution; exact filename varies by timestamp).

## Planned Functional Code Changes

- [ ] `peddlerr/workbench/scripts/reset_canonical_language.py` (create)
- [ ] `peddlerr/workbench/scripts/data/canonical_reset_snapshot_<timestamp>.json` (create — produced at script execution time)

## Implementation Plan

1. Inspect current state: query `core_semantics.canonical_concepts` and `mapping_definitions` row counts and surface any unexpected non-seed roots or non-Adena mappings. If unexpected state exists, surface to operator before proceeding.
2. Author the script with snapshot, pre-condition asserts, plan-print, `--confirm` gate, transactional purge, and post-state verification.
3. Run dry (no `--confirm`) and capture the plan output in the PR description.
4. Run with `--confirm`; capture the snapshot file path and post-state row counts in the PR description.
5. Re-run with `--confirm` to verify idempotency.

## Test Plan

### Testing Objective

Confirm the purge targets exactly the Adena-derived state, leaves source vocabulary untouched, and produces a clean slate for Tasks 8 and 9.

### Tests to Run

- `.venv/bin/python3 peddlerr/workbench/scripts/reset_canonical_language.py` (no flags) → plan-only output, no writes.
- `.venv/bin/python3 peddlerr/workbench/scripts/reset_canonical_language.py --confirm` → purge executes; post-state asserts pass.
- Re-run the same `--confirm` command → second run is a no-op (zero deletions).
- Sanity SQL queries:
  - `SELECT count(*) FROM core_semantics.canonical_concepts;` → equals number of seed roots.
  - `SELECT count(*) FROM core_semantics.mapping_definitions;` → 0.
  - `SELECT count(*) FROM semantic_modeling.candidate_terms;` → 0.
  - `SELECT count(*) FROM core_semantics.vocabulary_terms WHERE surface_id IN (SELECT id FROM core_semantics.vocabulary_surfaces WHERE vocabulary_model_id IN (SELECT id FROM core_semantics.source_vocabulary_models WHERE source_namespace = 'adena-health-system'));` → unchanged (Adena's published vocab is preserved).

### Success Criteria

- Plan-only run prints expected deletion counts; no DB rows change.
- `--confirm` run executes the purge; all post-state asserts pass.
- Re-run is a no-op.
- Snapshot file exists, contains pre-purge counts and (optionally) row-level data.
- Adena's source vocabulary is intact post-purge.

## Planned Test Files

None — operational script tested via inline runs against the live dev DB, consistent with project conventions.

## Verification

- [ ] Pre-purge snapshot captured and attached to PR.
- [ ] Plan-only run executed and output captured in PR.
- [ ] `--confirm` run executed; post-state verified.
- [ ] Re-run is a no-op.
- [ ] `vocabulary_terms` count for `adena-health-system` is unchanged.
